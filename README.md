# Custom Netboot Images Repository
This repository is for booting custom images on netboot.xyz!

# How It Works?
it works by using custom menu files (.ipxe) and allows us to boot custom portable OSes with custom scripts inside them to automate our installation.
